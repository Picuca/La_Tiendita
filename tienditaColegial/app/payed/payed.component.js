'use strict';

angular
    .module('payed')
    .component('payed', {
        templateUrl: 'payed/payed.template.html',

    }).controller('payedController', function ($scope, $location, $rootScope) {

});